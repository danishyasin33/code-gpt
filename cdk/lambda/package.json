{
  "name": "lambda",
  "version": "1.0.0",
  "description": "code for aws lambda that calls open API",
  "main": "code-gpt-lambda.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "transpile": "npx tsc --build tsconfig.json",
    "transpile-two": "npx tsc --skipLibCheck code-gpt-lambda.ts",
    "run-local": "node local-test",
    "run-ts": "npx ts-node local-tes.ts"
  },
  "author": "danish | maryam",
  "license": "ISC",
  "dependencies": {
    "openai": "^4.12.4"
  },
  "devDependencies": {
    "@types/node": "^20.8.6",
    "typescript": "^5.2.2"
  }
}